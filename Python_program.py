
#Defining a Function to print Squared Number
def print_square_value(numbers):
    for number in numbers:
        if number != 2:
            squared = number * number
            print(squared)

numbers = []
for x in range():
    n = input('Enter your List: \n')
    numbers.append(n)

print_square_value(numbers)